{
	"name": "jaaf",
	"version": "0.1.1",
	"description": "Just Another App Framework",
	"author": "UndyingWraith",
	"license": "MIT",
	"private": true,
	"type": "module",
	"packageManager": "yarn@4.6.0",
	"workspaces": [
		"modules/*"
	],
	"scripts": {
		"watch": "yarn workspaces foreach -Api run watch",
		"build": "yarn workspaces foreach -At run build",
		"test": "yarn workspaces foreach -At run test",
		"update": "yarn workspaces foreach -A version"
	},
	"devDependencies": {
		"typescript": "^5.7.3"
	}
}
